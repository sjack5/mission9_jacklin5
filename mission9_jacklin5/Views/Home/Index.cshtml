@{

}
@model BooksViewModel


@foreach (Book b in Model.Books)
{
    <div class="card card-outline-primary m-1 p-1">
        <div class="bg-faded p-1">
            <h4>
                @b.Title
                <span class="badge badge-pill badge-primary bg-dark" style="float:right">
                    <small>@b.Price.ToString("c")</small>
                </span>
            </h4>
        </div>
        <div class="card-text p-1">@b.Category</div>
    </div>

    <ul>
        <li>Author: @b.Author</li>
        <li>Publisher: @b.Publisher</li>            @*Lists information for each page, pulls from our queryable model*@
        <li>ISBN: @b.ISBN</li>
        <li>Classification: @b.Classification</li>
        <li>PageCount: @b.PageCount</li>
        <li>Price: $@b.Price</li>
    </ul>
}

@*Creates Pagination for us*@
<div page-blah="@Model.PageInfo" page-action="Index" page-classes-enabled="true"
     page-class="btn" page-class-normal="btn-outline-dark"
     page-class-selected="btn-primary" class="btn-group pull-right m-1">
</div>

@*<div page-blah="@Model.PageInfo" page-action="Index"></div>*@      